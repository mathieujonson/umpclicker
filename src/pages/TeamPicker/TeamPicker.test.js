it("has at least one test", () => {
  expect(true).toBe(true);
});

// const defaultProps = {};

// const render = overridingProps => {
//   const props = { ...defaultProps, ...overridingProps };
//   return shallow(<TeamPicker {...props} />);
// };

// describe("Team Picker", () => {
//   const wrapper = render();

//   it("renders without crashing", () => {
//     expect(wrapper.find(".team-picker").length).toEqual(1);
//   });
// });
